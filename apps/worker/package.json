{
  "name": "worker",
  "version": "1.0.0",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/consumers/vote.ts",
    "start": "node dist/consumers/vote.js",
    "build": "tsc",
    "test": "jest --config=jest.config.js --runInBand"
  }
}
